[pytest]
filterwarnings =
    ignore:Using h5py's default chunking with unlimited dimensions:FutureWarning
markers =
    local: run test only with local backends/files
    remote: run test only with remote (HSDS / h5pyd) backends
